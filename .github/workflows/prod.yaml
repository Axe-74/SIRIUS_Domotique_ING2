name: CI - Build & Deploy On Prod

on:
  workflow_dispatch:
  push:
    tags: 
      - R*

jobs:

  build_front:
    name: Build React Front
    runs-on: episen-domotique

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        working-directory: ./front
        run: npm install

      - name: Build front
        working-directory: ./front
        run: npm run build

      - name: Upload front artifact
        uses: actions/upload-artifact@v4
        with:
          name: front-build
          path: front/build

  build_backend:
    name: Build Spring Backend
    runs-on: episen-domotique

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build backend
        working-directory: ./back
        run: mvn clean package -DskipTests -PprodVM

      - name: Upload backend artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: back/target/*.jar

  deploy:
    name: Deploy to Integration Server
    runs-on: episen-domotique
    needs: [build_front, build_backend]

    steps:
      - name: Download front build
        uses: actions/download-artifact@v4
        with:
          name: front-build
          path: front-dist

      - name: Download backend jar
        uses: actions/download-artifact@v4
        with:
          name: backend-jar
          path: backend-dist

      - name: Copy front via SCP
        uses: easingthemes/ssh-deploy@main
        with:
          REMOTE_HOST: ${{ vars.PROD_FRONT_HOST }}
          REMOTE_USER: ${{ vars.PROD_FRONT_USER }}
          SSH_PRIVATE_KEY: ${{ secrets.PROD_FRONT_SSH_KEY }}
          SOURCE: "front-dist/*"
          TARGET: "/var/www/html"
          ARGS: '-zvci --delete'

      - name: Copy backend via SCP
        uses: easingthemes/ssh-deploy@main
        with:
          REMOTE_HOST: ${{ vars.PROD_API_HOST }}
          REMOTE_USER: ${{ vars.PROD_API_USER }}
          SSH_PRIVATE_KEY: ${{ secrets.PROD_API_SSH_KEY }}
          SOURCE: "backend-dist/*.jar"
          TARGET: "/opt/app/backend.jar"
          ARGS: '-zvci --delete'

      - name: Restart backend service
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ vars.PROD_API_HOST }}
          username: ${{ vars.PROD_API_USER }}
          key: ${{ secrets.PROD_API_SSH_KEY }}
          script: sudo systemctl restart domotique
